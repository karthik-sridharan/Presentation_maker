# Stage 36L Release Notes

- Fixed the generated HTML controls dropdown in the editor/export panel so checkbox labels are visible next to each box.
- Removed dependence on CSS pseudo-element fallback text for those option labels.
- Preserved Stage 36J generated-slide behavior, including fixed top-right optional controls and top-left Previous/Next navigation.

- Rebuilt the Generated HTML controls dropdown options as plain rows with explicit labels next to the checkboxes, plus a runtime Safari/iPad fallback.
