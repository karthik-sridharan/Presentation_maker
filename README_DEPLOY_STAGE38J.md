# Deploy Stage 38J

Deploy this folder as the site root. It is based on Stage 38I and adds only rail declutter CSS plus a small diagnostics decorator.

Recommended URL after deploy:

`index.html?v=stage38j-20260427-1&clearLuminaStorage=1`

Run diagnostics and check `stage38JRailDeclutterStatus.pass === true`.
