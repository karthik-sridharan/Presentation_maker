# Deploy Stage 38N

Deploy the folder contents as the site root. Use this cache-busting URL for testing:

`index.html?v=stage38n-20260427-1&clearLuminaStorage=1`

After load, copy diagnostics and confirm `stage38NIndependentPanelHeightsStatus.tests.pass` is true. Then add several slides and confirm the slide rail scrolls internally while the center preview and right inspector keep their proportions.
