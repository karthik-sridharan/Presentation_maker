# Deploy README — Stage 38F

Deploy this folder as the site root for Lumina Presenter.

Entrypoint:

- `index.html`

Stage identifiers:

- `window.LUMINA_STAGE = "stage38f-20260427-1"`
- `window.LUMINA_STAGE_SIGNATURE = "index-inline-stage38f-canvas-fit-regression-cleanup-20260427-1"`

Stage 38F is an inline canvas-fit cleanup over Stage 38E. No new external JS or CSS assets are required.

After deployment, run the diagnostic script against the deployed URL and confirm the Stage 38F canvas-fit block is present and passing.
