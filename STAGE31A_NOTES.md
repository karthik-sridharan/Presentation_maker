# Stage 31A notes

This stage adds optional ESM parity coverage for the deck/snippet layer, file/import workflow bridge, figure insertion helpers, and reusable block library.

The new files live under `js/esm/`. Keep the `js/esm/` subdirectory in the GitHub Pages deployment; otherwise the optional ESM probes will report 404s, while the classic app should still boot.
