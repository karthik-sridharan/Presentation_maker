# Stage 32A Notes

Adds optional ES module parity for editor-selection and block-editor helpers. The Stage 24C classic runtime remains authoritative; Stage 32A ESM files are imported only by the non-blocking diagnostics harness.
