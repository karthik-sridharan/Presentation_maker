# Release Notes — Stage 32A

Stage 32A adds optional ES module leaves for preview editor selection and block editor form/draft workflows. It should report 14 optional ESM assets and parity checks passing while preserving the Stage 24C startup path.
