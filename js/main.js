/**
 * main.js - Lumina Presenter modular entry point
 *
 * The app still runs through the compatibility layer in legacy-app.js while
 * we peel functionality into real modules. New modules should be imported
 * here, and legacy-app.js should shrink over time.
 */

import './legacy-app.js';
