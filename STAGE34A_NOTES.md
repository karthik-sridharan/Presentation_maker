# Stage 34A Notes

Stage 34A carries forward the working Stage 33A rail/empty-deck fixes and adds an optional ES module leaf for the command registry and keyboard-shortcut workflow. The Stage 24C classic runtime remains authoritative.

Expected diagnostics:

- `stage`: `stage34a-20260425-1`
- `optionalEsmAssetCount`: `17`
- `esModuleSmokePassed`: `true`
- New checks include `commands registry and shortcut workflow parity` and `commands module export createApi`.
