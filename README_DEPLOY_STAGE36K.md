# Lumina Presenter Stage 36K Deploy Package

Stage 36K is a hotfix over Stage 36J. It preserves the working generated HTML presentation behavior and fixes the editor export-controls dropdown so each checkbox displays its visible label text directly.

Deploy by uploading the contents of this folder to static hosting, preserving the `css/`, `js/`, and `prompts/` paths.
