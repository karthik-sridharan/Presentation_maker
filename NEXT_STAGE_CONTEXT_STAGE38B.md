# Next-stage context — Stage 38B

Stage 38B is the current clean deploy baseline. It keeps the Stage 38A canvas-first shell and adds a Keynote-style top toolbar. For Stage 38C, recommended next steps are inspector simplification and a Keynote-like format sidebar with grouped controls for slide, text, figure, theme, and export.

Important diagnostics to preserve:
- `stage38CanvasFirstStatus`
- `stage38KeynoteToolbarStatus`
- `stage37UiResetStatus`
- `esModuleSmokePassed`
