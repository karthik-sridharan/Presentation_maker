# Stage 38M deploy check

Open:

`index.html?v=stage38m-20260427-1&clearLuminaStorage=1`

Confirm:
- The top toolbar buttons above the preview feel like the small left rail buttons: compact, pill-shaped, and not visually dominant.
- The mode pill buttons (`Create`, `Edit`, `Present`, `Advanced`) are compact too.
- The preview remains lifted and canvas fit remains tight.
- A clean launch still seeds at least one slide into the deck/rail/outline.
- Diagnostics include `stage38MRailSizedTopToolbarStatus.tests.pass: true`.
