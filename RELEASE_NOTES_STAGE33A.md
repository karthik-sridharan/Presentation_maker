# Release Notes — Stage 33A

Stage 33A adds optional ES module leaves for the diagram editor popup API and interactive figure-tools workflows. It should report 16 optional ESM assets with parity checks passing while preserving the Stage 24C startup path.
