# Next stage context after Stage 38Q

Current UI direction:

- Canvas-first three-column layout remains the base shell.
- Copilot is staged in an AI drawer, not a normal left pane.
- Raw/debug/diagnostic surfaces are now Advanced-only.

Recommended next stage:

- Refine the AI drawer apply-preview flow with a richer visual slide preview of generated JSON, not only a text summary.
- Add a visible Advanced button/chip in the right inspector if diagnostics show users do not discover the top toolbar Advanced mode.
