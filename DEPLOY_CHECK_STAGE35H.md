# Stage 35H deploy check

1. Upload/replace `index.html` at the repository root.
2. Open:

```text
https://karthik-sridharan.github.io/Presentation_maker/index.html?v=stage35h-20260425-1&clearLuminaStorage=1
```

3. Confirm report fields:

```json
"stage": "stage35h-20260425-1",
"indexStageSignature": "index-inline-stage35h-late-template-tab-contrast-classic-palette-20260425-1",
"templateContrastStage": "stage35h-20260425-1",
"templateContrastComputed": { ... }
```

4. In Assets → Slide presets, check Core presets and Layout templates. Template cards should be light blue/gray with dark navy text; the active subtab should be dark blue with white text.
