# Deploy check — Stage 35S

Open with `?v=stage35s-20260425-1&clearLuminaStorage=1`.

Expected diagnostics:

- `stage`, `diagnosticScriptStage`, `stageFromWindow`, and `indexDatasetStage` are `stage35s-20260425-1`.
- `esModuleSmokePassed: true`.
- `missingAssets: []`, `missingGlobals: []`, `bootErrors: []`, `capturedErrors: []`.
- `featurePolishSummary.slideRailThumbnails: true`.
- `slideRailThumbnailStatus.thumbnailRail: true`.
- `slideRailThumbnailStatus.textOnlyDeckItems: 0`.
