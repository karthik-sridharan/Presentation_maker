# Deploy Check — Stage 36H

1. Open `index.html?v=stage36h-20260426-1&clearLuminaStorage=1`.
2. Open Save / export → Generated HTML controls.
3. Toggle each checkbox and verify the summary line updates without overflowing.
4. Save full presentation with all optional controls enabled.
5. Open the generated HTML and verify Previous/Next stay at top-left while Slides/Draw/Export annotated/Generate PDF/Pointer stay top-right.
6. Verify Generate PDF, Draw, Export annotated, and Pointer still work.
