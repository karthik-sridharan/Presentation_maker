Stage 12 renderer flat runtime patch

Copy the contents of this zip directly into your existing lumina-presenter project root.

Test with:
  http://localhost:8000/index.html?v=stage12-20260424-1

This patch extracts slide normalization/rendering helpers into js/renderer-stage12.js while keeping classic scripts and the stable non-module runtime.
