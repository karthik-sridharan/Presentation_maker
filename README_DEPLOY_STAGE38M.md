# Lumina Presenter — Stage 38M deploy package

Deploy the folder contents as the site root. Use a cache-busting URL while testing:

`index.html?v=stage38m-20260427-1&clearLuminaStorage=1`

Stage 38M is a visual-only toolbar refinement on top of Stage 38L. It does not add command hooks or change slide/deck behavior.
