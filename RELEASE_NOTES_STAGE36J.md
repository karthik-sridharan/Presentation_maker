# Stage 36J Release Notes

- Fixed Stage 36I generated HTML regression caused by an unescaped newline inside the generated viewer JavaScript.
- Preserved the fixed top-right optional-controls toolbar from Stage 36I.
- Strengthened the generated-HTML-controls dropdown labels so checkbox text remains visible.
