# Stage 36C deploy check

Suggested URL:

`index.html?v=stage36c-20260426-1&clearLuminaStorage=1`

Verify:

- `window.LUMINA_STAGE` is `stage36c-20260426-1`.
- Export panel checkbox label stays inside its box.
- Generated HTML presentations show Pointer dropdown options: Red, Blue, Green, Pointer, None.
- Pointer mode displays a classic black cursor arrow, not a black laser dot.
- None hides the pointer.
- Touch/pen single taps do not advance slides; nearby double taps or the top Next button advance.
- Desktop mouse click-to-advance still works.
