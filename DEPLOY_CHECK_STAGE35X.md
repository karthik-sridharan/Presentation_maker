# Stage 35X deploy check

Open:

```text
index.html?v=stage35x-20260426-1&clearLuminaStorage=1
```

Manual checks:

1. Add a diagram figure.
2. Move and resize it in the preview.
3. Double-click it or use **Edit figure**. Confirm the editor opens with a normal-size drawing canvas, not a tiny slide-scaled drawing.
4. Save back to slide. Confirm the figure keeps its edited SVG **and** keeps its previous position/size.
5. Drag and resize again. Confirm movement is smooth; it should only jump in grid increments if **Show grid** is enabled.
6. Confirm diagnostics still show no captured errors.
