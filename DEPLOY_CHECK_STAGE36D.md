# Stage 36D deploy check

Open after deployment with:

`index.html?v=stage36d-20260426-1&clearLuminaStorage=1`

Check:
- `window.LUMINA_STAGE` is `stage36d-20260426-1`.
- Export a standalone HTML presentation with live Draw enabled.
- Confirm the generated HTML slide toolbar shows `Slides`, `Draw`, `Export annotated slides`, `Generate PDF`, and `Pointer` on one top row.
- Confirm Pointer dropdown options are Red, Blue, Green, Pointer, and None.
- Confirm desktop click advances, while touch single tap does not advance and touch double tap does.
