# Stage 36B deploy check

Recommended URL:

`index.html?v=stage36b-20260426-1&clearLuminaStorage=1`

Verify:

- `window.LUMINA_STAGE` is `stage36b-20260426-1`.
- Export panel checkbox label stays inside its panel.
- Generated HTML has the Pointer menu: Red, Blue, Green, Black, None.
- On desktop/laptop, clicking the active slide advances.
- On touch screens, one tap on the slide does not advance; a nearby double tap advances.
- The top Next button advances on both desktop and touch.
