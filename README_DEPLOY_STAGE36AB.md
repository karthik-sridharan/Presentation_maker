# Stage 36AB clean deploy

Deploy the contents of this folder to the GitHub Pages repository root.

Test with:

```text
index.html?v=stage36ab-20260427-1&clearLuminaStorage=1
```

This stage fixes the real pixel font-size inspector so changing Font size (px) updates the preview and exported presentation.
